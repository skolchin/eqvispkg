.onLoad <- function(libname = find.package("eqvispkg"), pkgname = "eqvispkg"){

  # CRAN Note avoidance
  if(getRversion() >= "2.15.1")
    utils::globalVariables(
      c("%+replace%", "%>%", "DAY", "LATITUDE", "LOCATION_NAME", "LONGITUDE", "MONTH", "YEAR")
    )
}
